export const environment = {
  production: true,
  newsApiKey: 'NEWSAPI_KEY_PLACEHOLDER', // This will be replaced during build by GitHub Actions
  // Empty = same origin (e.g. https://newsaddaindia.com/api/...). Avoids mixed content and CORS when using the domain.
  apiUrl: ''
};

